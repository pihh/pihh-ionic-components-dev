import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pihh.components',
  appName: 'pihh-components',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
